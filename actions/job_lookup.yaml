ref: salt.job_lookup
label: "Look Up Salt Job"
description: "Fetch one validated Salt JID through /jobs/<jid>"
enabled: true
runner_type: python
runtime_version: ">=3.10"
entry_point: salt_action.py
parameter_delivery: stdin
parameter_format: json
output_format: json
default_execution_permission_set_refs: [privileged]
parameters:
  credential_key: {type: string, default: salt.credentials, required: true}
  jid: {type: string, required: true, pattern: "^[0-9]{14,24}$"}
output:
  operation: {type: string, required: true}
  data: {type: any, required: true}
  meta: {type: object, required: true}
  jid: {type: string}
  minions: {type: array}
tags: [salt, jobs, jid, privileged, read]
